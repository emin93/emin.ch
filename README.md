# emin.ch

Personal website for **Emin Khateeb**.

## Live profile

This site is intentionally minimal and terminal-themed. It highlights:

- Senior Software Engineer
- Primary focus: mobile app development
- Currently employed (not open to new opportunities)
- Ramping up in AI development
- Public links: GitHub, X/Twitter, Medium

## Tech

- Static HTML + CSS
- GitHub Pages deployment
- Custom domain configured via `CNAME`

## Local development

Open `index.html` directly in a browser, or run a static server in this folder.

Example:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.

## Deploy

Deployment is automatic via GitHub Pages from the `main` branch (repository root).
